plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace 'app.builderx.ogfa.androiduicomponents'
    compileSdk {
        version = release(36)
    }

    defaultConfig {
        applicationId "app.builderx.ogfa.androiduicomponents"
        minSdk 24
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

dependencies {
    implementation files('libs/native-views-release.aar')
    // Flat-file AARs cannot publish transitive dependencies. The parent app owns
    // Okio so it can share the version already selected by its OkHttp stack.
    implementation libs.okio
    implementation libs.appcompat
    implementation libs.material
    implementation libs.activity
    implementation libs.constraintlayout
    testImplementation libs.junit
    androidTestImplementation libs.ext.junit
    androidTestImplementation libs.espresso.core
}
